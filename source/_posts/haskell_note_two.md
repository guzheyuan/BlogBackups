---
layout: post
title: HASKELL学习笔记（二）
date: 2016-08-05 13:45
tags:
- haskell
---
## 类型类101
>类型定义行为的接口，如果一个类型属于某类型类，那它必实现了该类型类所描述的行为。很多从OOP走过来的人们往往会把类型类当成面向对象语言中的类而感到疑惑，厄，它们不是一回事。易于理解起见，你可以把它看做是java中接口（interface）的类似物。

直接来看下面这段代码<!--more-->
```haskell
ghci> :t (==)   
```
之前也说过了，判断相等的==运算符其实是一个函数（中缀函数），它的两个参数分别放在左右两边，我们把它用括号括起来，表示这个函数本身，然后用t函数去取他的类型，于是看到类型是这样的：
```haskell
ghci> :t (==)   
(==) :: (Eq a) => a -> a -> Bool
```
我们看到这里有个=>,那么这个=>怎么理解呢
>=>符号。它左边的部分叫做类型约束

所以(Eq a)就是一个类型约束，Eq就是一个类型类，a就是一个类型，a这个类型在Eq这个类型类中，所有在Eq这个类型类中的类型都有判断相等性的接口
>Eq这一类型类提供了判断相等性的接口，凡是可比较相等性的类型必属于Eq类。

所以(==) :: (Eq a) => a -> a -> Bool这句可以解读为：(==)是这样一个函数，他的参数是两个相同的类型a，它的返回是一个Bool值，类型a受到约束，必须有提供判断相等性的接口

类型类还有：
>**Ord**包含可比较大小的类型。除了函数以外，我们目前所谈到的所有类型都属于Ord类。Ord包中包含了<,>,<=,>=之类用于比较大小的函数。compare函数取两个Ord类中的相同类型的值作参数，返回比较的结果。这个结果是如下三种类型之一：GT,LT,EQ。
>
>**Show**的成员为可用字符串表示的类型。目前为止，除函数以外的所有类型都是Show的成员。操作Show类型类，最常用的函数表示show。它可以取任一Show的成员类型并将其转为字符串。
>
>**Read**是与Show相反的类型类。read函数可以将一个字符串转为Read的某成员类型。

等等

